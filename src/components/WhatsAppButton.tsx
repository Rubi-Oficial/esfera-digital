import { openChatbot } from "@/lib/chatbot-events";
import { motion } from "framer-motion";
import { MessageCircle } from "lucide-react";

const WhatsAppButton = () => {
  // Now this floating button opens the chatbot instead of WhatsApp directly
  // The chatbot handles qualification and routes to WhatsApp when appropriate
  return null; // Chatbot already has its own floating button
};

export default WhatsAppButton;
